{
  "name": "@scriptwizards/perhaps-web",
  "version": "0.1.0",
  "description": "Single-file browser demo of the perhaps engine",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@scriptwizards/perhaps-engine": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
