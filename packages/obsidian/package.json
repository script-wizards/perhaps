{
  "name": "@scriptwizards/perhaps-obsidian",
  "version": "0.1.0",
  "description": "Obsidian plugin: roll random tables written in plain text in your notes",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@scriptwizards/perhaps-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.2",
    "obsidian": "^1.8.7"
  }
}
